## 1. 桌面通用运行时边界

- [x] 1.1 新增桌面运行时平台能力判断入口，集中表达桌面通用与平台适配器边界。
- [x] 1.2 将 `main.dart` 中跨桌面运行时判断迁移到该入口，减少入口文件里的平台条件分散。
- [x] 1.3 保留 Windows 专属 window manager 初始化和单实例逻辑在 Windows 适配路径中。

## 2. 验证

- [x] 2.1 确认桌面数据库、托盘、单实例和窗口初始化仍按平台区分。
- [x] 2.2 运行静态分析或等效检查，确认改动没有破坏编译。
