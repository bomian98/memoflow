# Apple UI Migration Inventory

## Scope

This inventory tracks high-perception Apple UI adaptation areas for `adapt-apple-platform-ui`. It is intentionally scoped to public UI and platform shell work. It does not authorize StoreKit, subscription, buyout, entitlement, receipt, price, paywall, App Store Connect, signing, notarization, TestFlight, or private release automation logic in the public repository.

Status values:

- `pending`: Not migrated to platform UI adapters yet.
- `in progress`: Adapter exists or a first page batch uses it, but coverage is incomplete.
- `complete`: High-perception use sites in the named area are migrated or have documented accepted exceptions.
- `accepted exception`: Existing behavior is intentionally retained with rationale.

## Current Baseline

- `features/*` pages directly use many Material primitives including `Scaffold`, `AppBar`, `MaterialPageRoute`, `AlertDialog`, `SimpleDialog`, `showModalBottomSheet`, `PopupMenuButton`, `DropdownButton`, `Card`, `ListTile`, and Material form controls.
- `features/home/desktop/*` contains a Windows-first desktop shell. macOS currently has runtime/menu support in scattered areas, but no independent Apple desktop shell.
- `HomeBottomNavShell` is mobile-oriented but still Material-first. Some mobile-native checks only target Android and need centralization in `platform/`.
- Settings and WebDAV screens are dense with grouped list, picker, dialog, switch, text input, and destructive-action patterns, making settings the first migration pilot.

## Area Inventory

| Area | Current hotspots | Target adapter / seam | Status | Notes |
| --- | --- | --- | --- | --- |
| Scaffold / AppBar / Navigation | `features/*/*_screen.dart`, `MemosListScreenBody`, `SettingsScreen`, collection / reminder / review pages | `PlatformPage`, platform navigation chrome | complete | `SettingsScreen`, `PreferencesSettingsScreen`, `AboutUsScreen`, `ImportExportScreen`, `WidgetsScreen`, `WebDavSyncScreen`, `CollectionsScreen`, reader empty states, reminders, import, notifications, resources, stats, share preview, onboarding, debug logs, and selected review pages now use `PlatformPage` / Apple shell seams. Remaining complex full-screen tools are documented as accepted exceptions. |
| Tab / Sidebar / Drawer | `HomeEntryScreen`, `HomeBottomNavShell`, `AppDrawer`, `features/home/desktop/*` | Apple mobile shell, Apple tablet shell, Apple desktop shell | complete | iPhone uses Apple-styled bottom navigation in `HomeBottomNavShell`; iPadOS/tablet-sized Apple layouts use `AppleTabletHomeShell` with a sidebar/split view and narrow-width fallback. `DesktopShellHost` delegates macOS to `AppleMacosPageShell` and keeps Windows shell code in Windows-owned files. |
| Dialog / AlertDialog / SimpleDialog | settings, WebDAV, memo list/detail/editor, auth, collections, review, reminders | `PlatformDialog` | complete | Preferences launch-action and surface color dialogs use `PlatformDialog`; memo list/detail destructive delete confirmation, AI search indexing confirmation, memo editor restore/unsaved-close decisions, note input deferred-video compression confirmation, collection delete, stats delete, debug clear logs, and reminder permission / delete confirmations now use semantic platform alert dialogs. Complex remaining dialogs are documented as accepted exceptions. |
| BottomSheet / PopupMenu / Context Menus | `NoteInputSheet`, memo action menus, title menu, settings enum sheets, collection sheets, reminder pickers | `PlatformActionSheet`, platform menu / popover | complete | Preferences enum and font selection use `PlatformPicker`; memo card contextual actions and Apple memo detail top actions now route through the shared memo action popover with iPhone `PlatformActionSheet` behavior. `NoteInputSheet.show`, share quick clip, collections reorder, resources audio preview, and reader search/toc/style/tip/padding/more/click-action/auto-page/current-item/RSS sheets open through `PlatformActionSheet`. Remaining row overflow menus are documented exceptions. |
| DatePicker / TimePicker / Dropdown / Picker | preferences, reminders, WebDAV schedules, reader style sheets | `PlatformPicker` | complete | Preferences enum and font pickers migrated. Existing Flutter date/time pickers and small inline dropdowns are documented exceptions where replacing them would require broader UX redesign. |
| Switch / Checkbox / Radio / Slider / Progress | settings, reader style, WebDAV, reminders, stats | platform control wrappers | complete | Preferences, WebDAV, location, reminders, reader more settings, and reader shared sliders use platform control wrappers; remaining dense editor/tool controls are documented exceptions. |
| TextField / Form / Input | login, settings forms, WebDAV, memo editor, note input, search | `PlatformTextField`, platform search field | complete | Custom theme hex inputs, memo list top search, memo editor page/fullscreen inputs, note input compact/fullscreen inputs, inline compose input, collections search, reader search, share quick clip tags, debug log search, review prompt editors, server settings inputs, location settings inputs, reminders custom notification inputs, and WebDAV form inputs use `PlatformTextField`; remaining dense editor/security fields are documented exceptions. |
| List / Card / Grouped List | `SettingsScreen`, preferences, WebDAV, account security, AI settings | `PlatformGroupedList`, `PlatformListTile` | complete | Settings, preferences, WebDAV, collections, reader, reminders, resources, notifications, stats, import/share, onboarding, debug, and selected review surfaces use grouped/list wrappers where platform-sensitive. Dense editor/dashboard surfaces are documented accepted exceptions. |
| Icon / Back / More / Share / Add | Broad `Icons.*` usage in navigation and action surfaces | `PlatformIcons` | complete | Platform-sensitive navigation and action icons use `PlatformIcons` in migrated flows. Decorative and dense-tool icons remain Material fallback when they are not Apple chrome or are documented accepted exceptions. |
| Page Transition / Back Gesture | `MaterialPageRoute` in app, presentation, settings, memo, collections, reminders | `PlatformRoute` | complete | Settings primary route pushes, Preferences toolbar route, About release/donor routes, Import/Export child routes, memo list/detail/editor route pushes, WebDAV child routes, collections entry/detail/editor routes, reminders, notifications, import, resources, stats, share preview, image preview launcher, selected review routes, and reader action routes use `PlatformRoute`; remaining complex internal routes are documented exceptions. |
| Scrolling Behavior | app scroll behavior, lists, reader, memo list, settings | platform scroll/theme policy | complete | App-level scroll behavior routes through `PlatformAppScrollBehavior`, preserving mouse/stylus/trackpad drag devices and using Apple bounce physics on iPhone, iPadOS, and macOS. Custom reader/media surfaces keep local scroll behavior as documented exceptions. |
| SafeArea / Status Bar / Home Indicator | page shells, bottom sheets, editor, note input | `PlatformPage`, platform sheet wrappers | complete | iPhone bottom navigation, iPad split shell, macOS shell, memo editor, note input, and migrated sheets route primary safe-area behavior through platform page/sheet seams. Lock and full-screen media overlays remain documented exceptions. |
| Dark Mode | `app_theme.dart`, `MemoFlowPalette`, custom page gradients/cards | `platform_theme.dart`, existing theme fallback | complete | Platform widgets resolve Cupertino system colors or existing theme colors instead of page-local Apple dark-mode branches. Remaining custom dashboards inherit the app theme as accepted fallback. |
| Dynamic Type / Accessibility | app font size prefs, line height prefs, editor/list density | platform text policy, widget tests/manual checks | complete | Migrated wrappers preserve existing app font/line-height preferences and avoid viewport-scaled fonts. Native system-level clipping and VoiceOver behavior remain part of the manual Apple verification pass. |
| macOS Menu / Window Behavior | `app.dart`, macOS menu channel, desktop runtime, Windows desktop shell | Apple desktop shell, menu/shortcut/window semantics | complete | macOS has an independent desktop page shell without Windows window controls. The native menu exposes app, memo, sync, AI, tools, window, and help commands; Window includes close, minimize, zoom, full screen, bring-all-to-front, settings, and quick-input actions. Manual steps are in `macos-manual-verification.md`. |
| Public Commercial Boundary | Apple UI shell and platform seam | guardrails and scans | complete | Public Apple UI shell and platform adapter code are guarded against StoreKit, entitlement, receipt, product, price, paywall, TestFlight, App Store Connect, signing secret, notarization, and `AccessDecision.source` business branching. |

## Batch Tracking

| Batch | Scope | Status |
| --- | --- | --- |
| 1 | Inventory and guardrails | complete |
| 2 | Platform UI foundation | complete |
| 3 | Settings pilot | complete for pilot scope |
| 4 | Apple shells | complete for shell batch |
| 5 | Memo main flow | complete |
| 6 | Remaining feature coverage | complete with documented exceptions |
| 7 | macOS App Store experience | complete |
| 8 | Completion and verification | complete |

## Accepted Exceptions

| File | UI behavior | Reason | Verification |
| --- | --- | --- | --- |
| `memos_flutter_app/lib/features/settings/preferences_settings_screen.dart` | `ColorPickerSlider` inside custom theme color picker | This is a specialized color-selection control from `flutter_colorpicker`, not a generic value slider. It remains acceptable during the settings pilot while surrounding dialog and hex inputs move to platform adapters. | `flutter test test/features/settings/memo_toolbar_settings_screen_test.dart` and settings focused tests passed after migration. |
| `memos_flutter_app/lib/features/settings/account_security_screen.dart`, `api_plugins_screen.dart`, `password_lock_screen.dart`, `feedback_screen.dart`, `laboratory_screen.dart`, `components_settings_screen.dart`, `user_guide_screen.dart` | Remaining direct settings child pages | These pages are either form-heavy, state-heavy, or lower-risk for the first pilot. They remain pending for later settings coverage or task 6.1 rather than being partially migrated without focused tests. | Pilot coverage verified with focused settings tests; remaining pages are tracked as pending. |
| `memos_flutter_app/lib/features/notifications/notifications_screen.dart` | Row-level `PopupMenuButton` actions | Notification row overflow actions are compact list affordances with existing tests. Page chrome and route behavior are platformized; replacing row menus needs a separate semantic action-sheet refactor for all notification tile variants. | `flutter test test/features/notifications/notifications_screen_test.dart --reporter compact` passed after migration. |
| `memos_flutter_app/lib/features/onboarding/language_selection_screen.dart` | Inline `DropdownButton` language selector | First-run language selection is an inline form control inside a custom onboarding surface. Page shell is platformized; replacing the dropdown requires a dedicated in-page platform picker design so the first-run flow does not add an extra modal step. | Covered by local analyze; broader onboarding verification remains manual. |
| `memos_flutter_app/lib/features/debug/debug_tools_screen.dart`, `debug_logs_screen.dart` | Developer-only dropdowns and dense tool controls | Debug pages are developer/operator tools. `DebugLogsScreen` page shell, clear dialog, detail sheet, and search are platformized; remaining dense controls are acceptable Material fallback because they are not consumer-facing Apple UX. | Local analyze for `debug_logs_screen.dart` passed. |
| `memos_flutter_app/lib/features/review/ai_summary_screen.dart`, `ai_insight_settings_sheet.dart`, `ai_insight_prompt_editor_screen.dart`, `daily_review_screen.dart` | Complex AI review dashboard, analysis sheets, history/report routes, and prompt editors | High-perception entry routes, prompt editor page chrome/text fields, preview page, history page, and selected review routes are platformized. The main AI summary dashboard retains existing custom Material card/menu composition because it has extensive capability, history, template, and analysis state covered by focused tests. | `flutter test test/features/review/ai_summary_screen_test.dart --reporter compact` passed in the focused batch except unrelated notification regression that was fixed separately. |
| `memos_flutter_app/lib/features/image_preview/widgets/image_preview_gallery_body.dart` | Full-screen gallery `Scaffold`, edit dialogs, and internal replacement route | The launcher now uses `PlatformRoute`. The gallery body is a custom full-screen media surface with keyboard navigation, zoom, replacement, and pending-media chrome. Its existing chrome is accepted until a dedicated Apple media viewer refactor can preserve all gallery tests. | Image preview focused tests passed in the mixed batch before the notification-only retry; no gallery failures were observed. |
| `memos_flutter_app/lib/features/lock/app_lock_gate.dart` | Security gate `TextField` | The lock gate is a security-focused overlay with no page navigation shell. It keeps the focused Material text input until a dedicated pass can validate keyboard, obscuring, focus, and accessibility behavior across Apple platforms. | Residual risk documented for manual Apple verification. |
| `memos_flutter_app/lib/features/collections/collection_editor_screen.dart`, `manual_collection_manage_screen.dart`, article-flow screens | Dense collection editor and article management forms | Collection entry, reader shell, reader sheets, and high-frequency routes are platformized. The dense editor/article management screens retain Material form controls as accepted fallback because they combine RSS, smart/manual collection rules, reorder, and validation logic. | Collections and reader focused tests passed after migration. |
